# v1.2.2

## 版本简介

本版本将在线应用升级包从 `tar.zst` 切换为兼容性更好的 `tar.gz`，并把版本说明纳入代码版本管理。

## 主要变化

- 应用发行包统一使用 `tar.gz` 格式。
- 升级器使用 gzip 校验和解压发行包。
- 发布工作流要求 CI 全部通过后才发布签名资产。
- GitHub Release 读取同一 Tag 中的版本说明文件。

## 升级说明

从旧版本迁移时，需要先部署包含新版升级器的 Docker 镜像，再执行 `v1.2.2` 应用升级。
