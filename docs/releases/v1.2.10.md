# v1.2.10

## 版本简介

本版本统一控制台顶部品牌展示，使用简洁明确的 `CCFWP` 名称替代旧的 `Workers Console` 文案。

## 主要变化

- 将管理控制台顶部导航栏的品牌名称更新为 `CCFWP`。
- 保留现有 CCFWP 品牌图标、导航结构及功能行为不变。
- 改善产品名称一致性，便于管理员快速识别当前控制台。

## 升级说明

无需数据库迁移，也无需更新 Docker 镜像、Compose 配置或环境变量，可直接通过控制台在线升级。
