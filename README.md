# Cloudflare Local Platform

一个基于 Docker 的本地 Cloudflare 开发平台，允许你动态创建、部署和管理多个 Workers 和 Pages 项目。

## ✨ 特性

- **Web 管理界面**: 现代化的 Dashboard (React + Tailwind)。
- **动态部署**: 支持上传 JS (Worker) 或 HTML (Pages) 实时运行。
- **多项目管理**: 自动分配端口 (8000+)，进程隔离。
- **资源模拟**: 支持 D1 Database 和 Workers KV (配置已就绪，等待 UI 对接)。
- **持久化**: 项目文件和配置持久化存储于本地。

## 🚀 快速开始

### 前置要求
- Docker Desktop

### 启动

```bash
docker-compose up -d --build
```

启动后访问: **http://localhost:3000**

## 📂 目录结构

- `manager/`: 平台核心代码
  - `server.js`: Node.js 管理服务 (Express)。
  - `client/`: React 前端控制台。
  - `utils/`: 核心逻辑 (Config Generator, Process Spawner)。
- `.platform-data/`: (自动生成) 存储上传的代码和项目配置。

## 🛠 开发指南

如果你想修改平台本身：

1. **后端开发**:
   修改 `manager/server.js`，需要重启容器或在容器内使用 nodemon。

2. **前端开发**:
   ```bash
   cd manager/client
   npm install
   npm run dev
   ```
   这会启动本地 Vite 开发服务器，你可能需要配置代理指向 `http://localhost:3000` 以调试 API。
# cpcf-workers-pages
